import { Text, View } from "react-native";

export function BarcodeScanner() {
    return (
        <View>
            <Text>Barcode scanning is not supported on this platform.</Text>
        </View>
    );
}
